# vars() - Returns the variables associated with a specific object or type. Without arguments, it is equivalent to locals(), with arguments, it is equivalent to object.__dict__

print(vars())

print(vars(list))
