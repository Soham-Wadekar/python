# 10. Implement a function to calculate the area of a triangle given its base and height.


def area_of_triangle(b, h):
    return 0.5 * b * h


base = 10
height = 15

print(
    f"Area of triangle with base {base} and height {height} is {area_of_triangle(base, height)} sq. units"
)
