# 5. Write a program to remove duplicate elements from a list and convert it to a set.

lst = [1, 2, 3, 4, 5, 6, 2, 2, 4, 1, 3, 2, 6, 7, 8]

print(
    f"The list after removing duplicate elements and converted into a set: {set(lst)}"
)
